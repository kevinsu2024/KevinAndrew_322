#include <code_generator.h>
#include <unordered_map>

namespace IR {
    void
    generate_code(Program p, bool verbose){
        if (verbose){
            // return;
        }
        // return;
    }
}